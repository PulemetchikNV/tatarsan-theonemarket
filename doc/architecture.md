# 🏗️ Архитектура IT-Пульс Татарстана

## ⏰ Критические ограничения: 5 часов на всё

**Стратегия:** Максимальная параллелизация работы, минимум конфликтов, быстрый MVP

## 🎯 Обновленное распределение ролей

- **Андрей (ты):** Основной Backend (Node.js/TS + LangChain.js) → Frontend (последние 2 часа)
- **Данил:** Parser Service (PHP) - отдельный микросервис для парсинга данных
- **Минтимер:** LangChain Agents (TypeScript) - скидывает готовый код агентов Андрею для интеграции
- **Лиля:** Презентация и демо-сценарий

---

## 📁 Структура проекта (Микросервисная архитектура)

```
tatarsan-theonemarket/
├── docker-compose.yml           # Orchestration для всех сервисов
├── .env.example
│
├── backend/                     # 🟡 ЗОНА: Андрей - Main Backend API (Node.js/TS + LangChain.js)
│   ├── Dockerfile
│   ├── package.json
│   ├── tsconfig.json
│   │
│   ├── src/
│   │   ├── index.ts             # Entry point
│   │   │
│   │   ├── api/                 # REST API routes
│   │   │   ├── routes/
│   │   │   │   ├── companies.ts      # GET /api/companies
│   │   │   │   ├── vacancies.ts      # GET /api/vacancies
│   │   │   │   ├── metrics.ts        # GET /api/metrics
│   │   │   │   ├── technologies.ts   # GET /api/technologies
│   │   │   │   ├── ml.ts             # POST /api/ml/* - LangChain endpoints
│   │   │   │   └── users.ts          # User management
│   │   │   ├── controllers/
│   │   │   └── middleware/
│   │   │       ├── auth.ts
│   │   │       └── validation.ts
│   │   │
│   │   ├── services/            # Бизнес-логика
│   │   │   ├── company-service.ts
│   │   │   ├── vacancy-service.ts
│   │   │   ├── metrics-service.ts
│   │   │   ├── user-service.ts
│   │   │   ├── ai-service.ts         # Обертка над LangChain агентами
│   │   │   └── parser-integration-service.ts  # Интеграция с Parser
│   │   │
│   │   ├── langchain/           # 🟢 LangChain агенты (код от Минтимера)
│   │   │   ├── models/
│   │   │   │   ├── llm-config.ts     # LLM конфигурация
│   │   │   │   └── openai-client.ts
│   │   │   │
│   │   │   ├── agents/
│   │   │   │   ├── company-classifier.ts    # Классификация компаний
│   │   │   │   ├── tech-extractor.ts        # Извлечение tech stack
│   │   │   │   └── sentiment-analyzer.ts    # Sentiment analysis
│   │   │   │
│   │   │   ├── chains/
│   │   │   │   ├── analysis-chain.ts
│   │   │   │   └── classification-chain.ts
│   │   │   │
│   │   │   └── utils/
│   │   │       ├── prompts.ts
│   │   │       └── embeddings.ts
│   │   │
│   │   ├── database/            # Database layer
│   │   │   ├── models/          # ORM модели (Prisma/TypeORM)
│   │   │   │   ├── Company.ts
│   │   │   │   ├── Vacancy.ts
│   │   │   │   ├── Technology.ts
│   │   │   │   └── User.ts
│   │   │   ├── migrations/
│   │   │   └── seeds/           # Mock данные
│   │   │
│   │   ├── clients/             # HTTP клиенты для микросервисов
│   │   │   └── parser-client.ts      # Вызовы Parser Service
│   │   │
│   │   └── utils/
│   │       ├── logger.ts
│   │       ├── cache.ts
│   │       └── config.ts
│   │
│   └── scripts/
│       └── seed-db.ts           # Заполнение БД mock данными
│
├── parser-service/              # 🔵 ЗОНА: Данил - Parser Microservice (PHP)
│   ├── Dockerfile
│   ├── composer.json
│   │
│   ├── public/
│   │   └── index.php            # Entry point (slim/laravel micro)
│   │
│   ├── src/
│   │   ├── Parsers/
│   │   │   ├── HHParser.php         # HH.ru парсер
│   │   │   ├── GitHubParser.php     # GitHub парсер
│   │   │   ├── HabrParser.php       # Habr парсер
│   │   │   └── BaseParser.php       # Abstract класс
│   │   │
│   │   ├── Controllers/
│   │   │   └── ParserController.php  # API endpoints
│   │   │
│   │   ├── Models/              # Data models
│   │   │   ├── Company.php
│   │   │   ├── Vacancy.php
│   │   │   └── Technology.php
│   │   │
│   │   ├── Database/            # DB connection (shared with backend)
│   │   │   └── Connection.php
│   │   │
│   │   └── Utils/
│   │       ├── HttpClient.php
│   │       └── RateLimiter.php
│   │
│   └── routes/
│       └── api.php              # Parser API routes
│
├── frontend/                    # 🟡 ЗОНА: Андрей (последние 2 часа!)
│   ├── Dockerfile
│   ├── package.json
│   ├── vite.config.js
│   │
│   ├── public/
│   │   └── assets/
│   │
│   ├── src/
│   │   ├── main.js
│   │   ├── App.vue
│   │   │
│   │   ├── api/                 # API клиент
│   │   │   ├── client.js        # Axios конфиг
│   │   │   └── endpoints.js     # Все эндпоинты
│   │   │
│   │   ├── views/               # Страницы
│   │   │   ├── DashboardView.vue      # Главная с метриками
│   │   │   ├── CompaniesView.vue      # Список компаний
│   │   │   ├── VacanciesView.vue      # Вакансии
│   │   │   └── TechnologiesView.vue   # Технологии
│   │   │
│   │   ├── components/          # Компоненты
│   │   │   ├── charts/
│   │   │   │   ├── MetricsChart.vue
│   │   │   │   ├── SalaryChart.vue
│   │   │   │   └── TechStackChart.vue
│   │   │   ├── ui/
│   │   │   │   ├── SearchBar.vue
│   │   │   │   ├── FilterPanel.vue
│   │   │   │   └── CompanyCard.vue
│   │   │   └── layout/
│   │   │       ├── Header.vue
│   │   │       └── Sidebar.vue
│   │   │
│   │   ├── stores/              # Pinia stores
│   │   │   ├── companies.js
│   │   │   ├── vacancies.js
│   │   │   └── metrics.js
│   │   │
│   │   ├── router/
│   │   │   └── index.js
│   │   │
│   │   └── utils/
│   │       ├── formatters.js
│   │       └── constants.js
│   │
│   └── tests/
│
├── presentation/                # 🟣 ЗОНА: Лиля (полная изоляция!)
│   ├── slides/
│   │   └── main.pptx
│   ├── assets/
│   │   ├── screenshots/
│   │   └── diagrams/
│   └── demo-script.md
│
└── docs/
    ├── tz.md
    ├── todo.md
    ├── architecture.md          # Этот файл
    └── api-spec.md              # API контракт
```

---

## 🎯 Изоляция зон ответственности (НЕТ КОНФЛИКТОВ!)

### 🟡 Андрей - Main Backend + Frontend
- **Папки:** `backend/` (100% изоляция), `frontend/` (последние 2 часа)
- **Работа:**
  - **Часы 1-3:** Основной Backend API (Node.js/TS + LangChain.js) - архитектура, роуты, сервисы, интеграция LangChain агентов от Минтимера
  - **Часы 4-5:** Frontend (Vue.js) - Dashboard, компоненты, графики
- **Брейншторм:** Архитектура пользователей, структура данных, API контракт
- **Конфликты:** НОЛЬ (полная изоляция обеих папок)

### 🔵 Данил - Parser Microservice (PHP)
- **Папка:** `parser-service/` - только он туда заходит
- **Работа:** Отдельный микросервис на PHP для парсинга HH.ru, GitHub, Habr
- **API:** Предоставляет HTTP endpoints для основного Backend
- **База:** Работает с той же БД, что и основной Backend
- **Конфликты:** НОЛЬ (полная изоляция)

### 🟢 Минтимер - LangChain Agents (TypeScript)
- **Работа:** LangChain агенты (company classifier, tech extractor, sentiment) - работает в отдельной папке/проекте
- **Интеграция:** Скидывает готовый код агентов Андрею для интеграции в `backend/src/langchain/`
- **Не создает:** Отдельный микросервис - все агенты встраиваются в основной Backend
- **Конфликты:** Минимальные (только когда передает код Андрею)

### 🟣 Лиля - Presentation
- **Папка:** `presentation/` - полная изоляция
- **Работа:** Презентация, демо-сценарий
- **Конфликты:** НОЛЬ

---

## 🔄 Workflow: Как работаем параллельно

### Этап 0: Подготовка (первые 15 минут, ВСЕ ВМЕСТЕ)
1. **Создаем `api-spec.md`** - API контракт (эндпоинты + структура данных)
2. **Создаем mock данные** - sample JSON для фронтенда
3. **Настраиваем Docker Compose** - все поднимается одной командой
4. **Договариваемся о структурах данных** - Company, Vacancy, Metrics

```json
// Пример контракта
GET /api/metrics
{
  "totalCompanies": 150,
  "totalVacancies": 450,
  "avgSalary": 120000,
  "topTechnologies": ["Python", "JavaScript", "Go"],
  "activityIndex": 87
}
```

### Этап 1: Параллельная работа (4.5 часа)

#### 🟡 Андрей (Backend + LangChain → Frontend):
- **Час 1 (Брейншторм + Setup):** 
  - Архитектура пользователей (нужна ли авторизация?)
  - Структура данных (Company, Vacancy, Technology, Metrics)
  - API контракт с Parser Service
  - Setup проекта: Node.js/TS, Express/Fastify, ORM, LangChain.js
  
- **Час 2 (Backend Core):**
  - Настройка БД + модели (Companies, Vacancies, Technologies, Users если нужно)
  - Основные API endpoints (возвращают моки/seeded данные)
  - HTTP клиент для Parser Service
  - Базовая настройка LangChain.js
  
- **Час 3 (Backend Integration + LangChain):**
  - Интеграция LangChain агентов от Минтимера в backend/src/langchain/
  - ML API endpoints (classify-company, extract-tech-stack)
  - Metrics service (агрегация статистики)
  - Кэширование (Redis) для быстрых ответов
  - Mock данные для демо
  
- **Час 4 (Frontend - Dashboard):**
  - Setup Vue + Vite + Pinia
  - Dashboard с ключевыми метриками (графики)
  - API клиент + подключение к Backend
  
- **Час 5 (Frontend - Polish):**
  - Список компаний и вакансий
  - Поиск и фильтры
  - Адаптивный дизайн
  - Финальная полировка UI

#### 🔵 Данил (Parser Service - PHP):
- **Час 1:** 
  - Setup PHP проекта (Slim/Laravel micro)
  - Подключение к БД
  - Базовая структура парсеров
  
- **Час 2:** 
  - HH.ru парсер (самый важный!)
  - Парсинг вакансий по Татарстану
  - Сохранение в БД
  
- **Час 3:** 
  - GitHub парсер (упрощенный)
  - Habr парсер (упрощенный)
  
- **Час 4:** 
  - API endpoints для запуска парсеров
  - Обработка ошибок и rate limiting
  - Интеграция с основным Backend

#### 🟢 Минтимер (LangChain Agents):
- **Час 1-2:** 
  - Setup LangChain.js в отдельной папке
  - Подключение LLM модели (OpenAI/local)
  - Агент классификации компаний (fintech/AI/gaming/etc)
  - Промпты и chains
  
- **Час 3:** 
  - Агент извлечения tech stack из вакансий
  - NLP анализ текстов
  - Экспорт функций для интеграции
  
- **Час 4:** 
  - Передача кода Андрею (скидывает в backend/src/langchain/)
  - Sentiment analysis (опционально)
  - Совместное тестирование с Андреем
  - Проверка работы агентов в Backend API

#### 🟣 Лиля (Presentation):
- **Часы 1-2:** Презентация проекта (слайды)
- **Час 3:** Архитектурные диаграммы + сценарий демо
- **Час 4:** Финальная сборка презентации + репетиция

### Этап 2: Интеграция и тестирование (последние 30 минут)
- **Андрей:** Проверка всех интеграций (Parser Service + LangChain агенты)
- **Данил:** Запуск парсеров и заполнение БД реальными данными
- **Минтимер:** Тестирование LangChain агентов с реальными данными из БД
- **Лиля:** Финальные скриншоты готового продукта
- **ВСЕ:** Smoke тест всей системы, подготовка к демо

---

## 🐳 Docker Compose структура (2 микросервиса)

### docker-compose.yml

```yaml
version: '3.8'

services:
  # База данных (общая для всех сервисов)
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: itpulse
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis для кэширования
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    networks:
      - app-network

  # Main Backend API (Node.js/TS + LangChain.js) - Андрей
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgresql://admin:password@db:5432/itpulse
      REDIS_URL: redis://redis:6379
      NODE_ENV: development
      PARSER_SERVICE_URL: http://parser-service:8080
      OPENAI_API_KEY: ${OPENAI_API_KEY}  # Для LangChain.js
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    volumes:
      - ./backend:/app
      - /app/node_modules
    networks:
      - app-network
    command: npm run dev

  # Parser Service (PHP) - Данил
  parser-service:
    build:
      context: ./parser-service
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      DATABASE_URL: postgresql://admin:password@db:5432/itpulse
      PHP_ENV: development
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./parser-service:/var/www/html
    networks:
      - app-network
    command: php -S 0.0.0.0:8080 -t public

  # Frontend (Vue.js) - Андрей (последние 2 часа)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    environment:
      VITE_API_URL: http://localhost:3000/api
    depends_on:
      - backend
    volumes:
      - ./frontend:/app
      - /app/node_modules
    networks:
      - app-network
    command: npm run dev

volumes:
  postgres_data:

networks:
  app-network:
    driver: bridge
```

---

## 🔌 API Endpoints (контракт для параллельной работы)

### Main Backend API (Port 3000) - Public endpoints

```
# Core API
GET    /api/metrics              # Ключевые метрики IT-экосистемы
GET    /api/companies            # Список компаний + фильтры
GET    /api/companies/:id        # Детали компании
GET    /api/vacancies            # Список вакансий + фильтры
GET    /api/vacancies/:id        # Детали вакансии
GET    /api/technologies         # Топ технологий + статистика
GET    /api/search               # Универсальный поиск

# LangChain ML endpoints (встроены в Backend)
POST   /api/ml/classify-company      # Классификация компании
POST   /api/ml/extract-tech-stack    # Извлечение технологий из текста
POST   /api/ml/analyze-sentiment     # Sentiment analysis

# User management (если нужно)
POST   /api/auth/register        # Регистрация
POST   /api/auth/login           # Авторизация
GET    /api/users/me             # Профиль пользователя
```

### Parser Service API (Port 8080) - Internal endpoints

```
POST   /parse/hh                 # Запуск парсера HH.ru
POST   /parse/github             # Запуск парсера GitHub
POST   /parse/habr               # Запуск парсера Habr
POST   /parse/all                # Запуск всех парсеров
GET    /parse/status             # Статус парсеров
```

---

## 🚀 Dockerfile примеры

### backend/Dockerfile (Node.js/TypeScript)

```dockerfile
FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 3000

CMD ["npm", "run", "dev"]
```

### parser-service/Dockerfile (PHP)

```dockerfile
FROM php:8.2-cli-alpine

# Установка расширений PHP
RUN apk add --no-cache postgresql-dev \
    && docker-php-ext-install pdo pdo_pgsql

WORKDIR /var/www/html

# Установка Composer
COPY --from=composer:latest /usr/bin/composer /usr/bin/composer

COPY composer.json composer.lock ./
RUN composer install --no-dev --optimize-autoloader

COPY . .

EXPOSE 8080

CMD ["php", "-S", "0.0.0.0:8080", "-t", "public"]
```

### frontend/Dockerfile (Vue.js)

```dockerfile
FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 5173

CMD ["npm", "run", "dev"]
```

---

## 🎯 Критические точки синхронизации

### 1. API Контракт (создать ПЕРВЫМ!)
- **Кто:** Андрей (брейншторм) → все согласовывают
- **Формат:** `docs/api-spec.md` с примерами запросов/ответов
- **Зачем:** Все знают какие эндпоинты и структуры данных использовать
- **Время:** Первые 30 минут

### 2. Модели данных (Database Schema)
- **Кто:** Андрей определяет структуру БД
- **Формат:** Prisma schema / SQL migrations
- **Зачем:** Все работают с одинаковыми таблицами и типами
- **Время:** Первые 30 минут

### 3. Mock данные
- **Кто:** Андрей создает `backend/src/database/seeds/mock-data.ts`
- **Зачем:** Фронтенд и демо работают с красивыми данными
- **Время:** Час 2-3

### 4. Микросервисная интеграция
- **Parser Service (Данил):** REST API на порту 8080
- **Main Backend (Андрей):** Orchestrator + встроенные LangChain агенты, вызывает Parser Service
- **LangChain Agents (Минтимер):** Код агентов интегрируется в Backend (backend/src/langchain/)
- **Зачем:** Полная изоляция Parser Service, LangChain работает внутри Backend

---

## ⚡ Оптимизация для 5 часов

### Что делаем:
- ✅ Один основной парсер (HH.ru) - самый важный источник данных
- ✅ Упрощенные GitHub/Habr парсеры (базовая функциональность)
- ✅ 2-3 ключевых ML агента (классификация + tech stack)
- ✅ Dashboard с основными метриками и графиками
- ✅ Mock данные для красивой демонстрации
- ✅ Docker Compose для быстрого разворачивания

### Что НЕ делаем (оставляем на потом):
- ❌ Real-time обновления (WebSocket)
- ❌ Мобильное приложение
- ❌ Сложная аналитика и прогнозы
- ❌ Систему уведомлений
- ❌ Production-ready инфраструктуру
- ❌ Тесты (если совсем не успеваем)

---

## 📊 Приоритеты для каждой зоны

### Андрей (Backend + Frontend):
**Backend (Часы 1-3):**
1. **Критично:** API endpoints + модели БД + интеграция микросервисов
2. **Важно:** Mock данные для демо + кэширование
3. **Nice to have:** User management система

**Frontend (Часы 4-5):**
1. **Критично:** Dashboard с графиками (даже с mock data выглядит WOW!)
2. **Важно:** Список компаний и вакансий
3. **Nice to have:** Поиск и детальные страницы

### Данил (Parser Service - PHP):
1. **Критично:** Парсер HH.ru (главный источник данных!)
2. **Важно:** Сохранение в БД + API endpoints для запуска
3. **Nice to have:** GitHub и Habr парсеры

### Минтимер (LangChain Agents):
1. **Критично:** Агент классификации компаний по индустриям (TypeScript + LangChain.js)
2. **Важно:** Агент извлечения tech stack из текста вакансий
3. **Nice to have:** Sentiment analysis агент
4. **Передача кода:** Скинуть готовые агенты Андрею для интеграции

### Лиля (Presentation):
1. **Критично:** Презентация проекта
2. **Важно:** Демо-сценарий
3. **Nice to have:** Архитектурные диаграммы

---

## 🎬 План демонстрации

1. **Вступление** (Лиля) - проблема и решение
2. **Архитектура** (Данил) - как устроена система (2 микросервиса)
3. **Live Demo** (Андрей) - показ работающего Dashboard + LangChain агенты в действии
4. **AI возможности** (Минтимер) - как работают LangChain агенты (классификация, извлечение tech stack)
5. **Заключение** (Лиля) - планы развития

---

## 🔥 Checklist запуска

```bash
# 1. Клонируем репо
git clone <repo>
cd tatarsan-theonemarket

# 2. Создаем .env файл
cp .env.example .env

# 3. Поднимаем все сервисы
docker-compose up -d

# 4. Проверяем что все работает
curl http://localhost:3000/api/metrics    # Backend API (+ LangChain)
curl http://localhost:8080/parse/status   # Parser Service
open http://localhost:5173                # Frontend

# 5. Запускаем парсеры (заполняем БД)
docker-compose exec backend npm run parse

# 6. Открываем Dashboard и наслаждаемся!
```

---

**🎯 Главное правило:** Каждый работает в своей зоне. Андрей - Backend+Frontend, Данил - Parser Service, Минтимер - LangChain агенты (скидывает код Андрею). Минимум коммуникации после первых 15 минут. Максимум параллелизма. Успеем за 5 часов!
