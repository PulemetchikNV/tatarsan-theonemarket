# IT-Пульс Татарстана - Backend

Бэкенд приложения с 7 AI-агентами на базе LangChain.js для анализа IT-экосистемы Татарстана.

## 🏗️ Архитектура

```
backend/
├── src/
│   ├── index.ts                 # Entry point
│   │
│   ├── core/
│   │   ├── server.ts            # Fastify server setup
│   │   │
│   │   ├── api/                 # REST API
│   │   │   ├── routes.ts        # Route definitions
│   │   │   └── handlers.ts      # Request handlers
│   │   │
│   │   ├── langchain/           # LangChain агенты
│   │   │   └── agents/
│   │   │       ├── baseAgent.ts              # Базовый класс
│   │   │       ├── orchestratorAgent.ts      # 🎯 Главный координатор
│   │   │       ├── dataCollectorAgent.ts     # 1. Сбор данных
│   │   │       ├── analyzerAgent.ts          # 2. Анализ данных
│   │   │       ├── industryClassifierAgent.ts # 3. Классификация
│   │   │       ├── marketResearcherAgent.ts  # 4. Исследование рынка
│   │   │       ├── eventTrackerAgent.ts      # 5. Отслеживание событий
│   │   │       ├── alerterAgent.ts           # 6. Генерация алертов
│   │   │       └── reportGeneratorAgent.ts   # 7. Генерация отчетов
│   │   │
│   │   ├── types/               # TypeScript типы
│   │   │   └── index.ts
│   │   │
│   │   ├── utils/               # Утилиты
│   │   │   └── logger.ts
│   │   │
│   │   └── services/            # Бизнес-логика (из старого проекта)
│   │
│   └── ...
```

## 🤖 7 AI-Агентов

### Orchestrator Agent (Координатор)
Главный агент, координирующий работу всех остальных агентов.

**Основные методы:**
- `analyzeCompany(companyName, deepAnalysis)` - полный анализ компании
- `getDashboard()` - получение данных для дашборда

### 1. Data Collector Agent
Собирает данные из разных источников:
- HH.ru (вакансии)
- GitHub (репозитории, активность)
- Habr (статьи, упоминания)
- LinkedIn (профиль компании)

### 2. Analyzer Agent
Анализирует собранные данные:
- NLP-анализ текстов
- Sentiment analysis
- Определение сильных/слабых сторон
- Оценка качества tech stack

### 3. Industry Classifier Agent
Классифицирует компанию:
- Primary/secondary industries (fintech, AI, gaming, etc)
- Стадия развития (idea, pre-seed, seed, growth, mature)
- Confidence score

### 4. Market Researcher Agent
Исследует рынок:
- Анализ трендов технологий
- Спрос на специалистов
- Динамика зарплат
- Потенциал роста

### 5. Event Tracker Agent
Отслеживает события:
- Конференции, митапы, хакатоны
- Инвестиционные раунды
- Запуски продуктов
- Упоминания в новостях

### 6. Alerter Agent
Генерирует персонализированные алерты:
- Критичные изменения
- Новые возможности
- Важные события

### 7. Report Generator Agent
Генерирует отчеты:
- Краткое резюме
- Детальный отчет
- Рекомендации для инвесторов

## 🚀 API Endpoints

### POST `/api/v1/analyze`
Анализ компании с помощью всех 7 агентов.

**Request:**
```json
{
  "companyName": "Таттелеком",
  "deepAnalysis": true
}
```

**Response:**
```json
{
  "success": true,
  "data": {
    "company": {
      "name": "Таттелеком",
      "techStack": ["Java", "PostgreSQL", "Kubernetes"]
    },
    "healthScore": 85,
    "recommendation": "invest",
    "reasoning": "Компания показывает высокую активность...",
    "dataCollector": { ... },
    "analyzer": { ... },
    "industryClassifier": { ... },
    "marketResearcher": { ... },
    "eventTracker": { ... },
    "timestamp": "2024-11-22T..."
  }
}
```

### GET `/api/v1/dashboard`
Получение данных для дашборда.

**Response:**
```json
{
  "success": true,
  "data": {
    "overview": {
      "totalCompanies": 127,
      "totalVacancies": 485,
      "avgSalary": 165000,
      "topTechnologies": ["TypeScript", "React", "Python"],
      "growthRate": 12.5
    },
    "topCompanies": [ ... ],
    "techTrends": [ ... ],
    "salaryStats": { ... },
    "activityIndex": 78
  }
}
```

### GET `/health`
Health check endpoint.

## 🛠️ Установка и запуск

### 1. Установка зависимостей
```bash
npm install
```

### 2. Настройка окружения
```bash
cp env.example .env
# Отредактируйте .env и добавьте OPENAI_API_KEY
```

### 3. Запуск в dev режиме
```bash
npm run dev
```

### 4. Сборка для production
```bash
npm run build
npm start
```

### 5. Docker
```bash
docker build -t itpulse-backend .
docker run -p 3000:3000 --env-file .env itpulse-backend
```

## 🔧 Конфигурация

### Переменные окружения

| Переменная | Описание | Значение по умолчанию |
|------------|----------|----------------------|
| `PORT` | Порт сервера | `3000` |
| `NODE_ENV` | Окружение | `development` |
| `OPENAI_API_KEY` | API ключ OpenAI | - |
| `OPENAI_MODEL` | Модель OpenAI | `gpt-4o-mini` |
| `PARSER_SERVICE_URL` | URL Parser Service | `http://parser-service:8080` |
| `LOG_LEVEL` | Уровень логирования | `info` |

## 📊 Workflow анализа компании

```
1. Пользователь → POST /api/v1/analyze { companyName: "X" }
   
2. Orchestrator Agent запускает процесс:
   
   ┌─────────────────────────────────────────┐
   │  STEP 1: Data Collector Agent           │
   │  Собирает данные из HH, GitHub, Habr    │
   └─────────────────────────────────────────┘
                    ↓
   ┌─────────────────────────────────────────┐
   │  STEPS 2-5: Parallel Analysis           │
   │  - Analyzer Agent                       │
   │  - Industry Classifier Agent            │
   │  - Market Researcher Agent              │
   │  - Event Tracker Agent                  │
   └─────────────────────────────────────────┘
                    ↓
   ┌─────────────────────────────────────────┐
   │  STEP 6: Orchestrator Final Decision    │
   │  Health Score + Recommendation          │
   └─────────────────────────────────────────┘
                    ↓
   ┌─────────────────────────────────────────┐
   │  STEP 7: Background Tasks               │
   │  - Alerter Agent (alerts)               │
   │  - Report Generator Agent (reports)     │
   └─────────────────────────────────────────┘
                    ↓
3. Response → { healthScore, recommendation, ... }
```

## 🧪 Тестирование

### Запуск тестов
```bash
npm test
```

### Пример curl запроса
```bash
# Анализ компании
curl -X POST http://localhost:3000/api/v1/analyze \
  -H "Content-Type: application/json" \
  -d '{"companyName": "Таттелеком", "deepAnalysis": true}'

# Получение дашборда
curl http://localhost:3000/api/v1/dashboard
```

## 📝 TODO для развития

- [ ] Интеграция с реальным Parser Service (PHP)
- [ ] Кэширование результатов анализа (Redis)
- [ ] WebSocket для real-time обновлений
- [ ] Rate limiting для API
- [ ] Авторизация пользователей
- [ ] Сохранение результатов анализа в БД
- [ ] Исторические данные и тренды
- [ ] Экспорт отчетов (PDF, Excel)

## 🎯 MVP Features (для хакатона)

- ✅ 7 AI-агентов на LangChain.js
- ✅ Orchestrator для координации
- ✅ REST API (Fastify)
- ✅ Полный анализ компании за ~30 секунд
- ✅ Dashboard данные
- ✅ Mock данные для демо
- ⏳ Интеграция с Parser Service
- ⏳ Frontend для визуализации

## 🔗 Связанные сервисы

- **Parser Service** (PHP) - `/parser-service/` - парсинг HH.ru, GitHub, Habr
- **Frontend** (Vue.js) - `/frontend/` - Dashboard и визуализация

---

**Команда:** The One Market  
**Проект:** IT-Пульс Татарстана

