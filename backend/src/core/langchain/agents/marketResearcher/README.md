# Market Researcher Agent

## 🎯 Назначение

ДУМАЮЩИЙ агент для исследования IT-рынка Татарстана.

**Особенность:** Использует AI + LangChain tools. САМ решает какие исследования проводить.

## 🧠 Возможности

- ✅ Анализирует рыночные тренды
- ✅ Оценивает спрос на технологии
- ✅ Исследует конкурентную среду
- ✅ Определяет потенциал роста компании
- ✅ САМ выбирает нужные инструменты

## 🔐 Контракт

```typescript
// Input
companyName: string
collectedData: DataCollectorResult

// Output
MarketResearcherResult {
  marketTrends: string[]
  demandForTech: Record<string, number>  // tech -> demand (0-100)
  competitorAnalysis: string
  growthPotential: number  // 0-100
}
```

## 🛠️ Внутренние Tools

Агент использует 3 инструмента:

### 1. `research_market`
Проводит комплексное рыночное исследование:
- Собирает рыночные тренды
- Анализирует конкурентов
- Оценивает потенциал роста

### 2. `get_top_technologies`
Получает топ технологий по спросу:
```typescript
Input:  { limit?: number }
Output: Record<string, number>  // tech -> demand
```

### 3. `get_tech_demand`
Проверяет спрос на конкретную технологию:
```typescript
Input:  { technology: string }
Output: number  // demand score 0-100
```

## 💡 Использование

```typescript
import { marketResearcherAgent } from './agents/marketResearcher';

// collectedData получили от DataCollectorAgent
const research = await marketResearcherAgent.research('Иннополис', collectedData);

console.log('Тренды:', research.marketTrends);
console.log('Потенциал роста:', research.growthPotential);
console.log('Спрос на TypeScript:', research.demandForTech['TypeScript']);
```

## 🏗️ Архитектура

```
marketResearcher/
├── index.ts              # MarketResearcherAgent (ThinkingAgent)
├── tools/                # Внутренние tools для агента
│   └── index.ts          # Экспорты (импортирует из общих tools)
└── README.md            # Эта документация
```

## 🤖 Как работает AI

```
User: research("Иннополис", data)
    ↓
Agent думает: "Нужно исследовать рынок"
    ↓
Agent вызывает: research_market(Иннополис)
    ↓
Получает: {trendsCount: 10, growthPotential: 85}
    ↓
Agent думает: "Теперь топ технологии"
    ↓
Agent вызывает: get_top_technologies(10)
    ↓
Получает: {TypeScript: 95, Python: 92, ...}
    ↓
Agent думает: "Проверю спрос на Python"
    ↓
Agent вызывает: get_tech_demand("Python")
    ↓
Получает: 92
    ↓
Agent анализирует все данные
    ↓
Возвращает: MarketResearcherResult
```

## 🔗 Интеграция с Orchestrator

Используется через `orchestrator/tools/researchMarketTool.ts`:

```typescript
researchMarketTool -> marketResearcherAgent.research()
                   -> MarketResearcherResult
                   -> wrapped in MarketResearchOutput
```

## 📊 Стратегия исследования

Агенту даны четкие инструкции в system prompt:

1. Начни с `research_market` для общей картины
2. Используй `get_top_technologies` для понимания рынка
3. Если есть специфичные технологии - используй `get_tech_demand`
4. Сопоставь tech stack компании с рыночным спросом

Агент САМ решает когда и какие инструменты вызывать!

## 🎓 Тип агента

**ThinkingAgent** (базовый класс для думающих агентов):
- Использует `createAgent` с tools
- САМ принимает решения
- Может делать reasoning
- Адаптивен к ситуации

## 📝 Roadmap

- [ ] Улучшить парсинг LangChain response
- [ ] Добавить tool для анализа зарплатных трендов
- [ ] Добавить tool для прогнозирования роста
- [ ] Добавить кэширование рыночных данных
- [ ] Интеграция с внешними аналитическими API

## 🔥 Ключевое отличие от простых агентов

| Простой агент | MarketResearcher (ThinkingAgent) |
|---------------|----------------------------------|
| Жесткая логика | САМ решает |
| Нет tools | 3 tools на выбор |
| Всегда один путь | Адаптивен |
| Не рассуждает | Reasoning loop |

Это позволяет агенту быть гибким и эффективным!

